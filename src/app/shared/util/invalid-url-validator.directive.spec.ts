import { InvalidUrlValidatorDirective } from './invalid-url-validator.directive';

describe('InvalidUrlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidUrlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
